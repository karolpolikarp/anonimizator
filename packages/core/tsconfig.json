// Typecheck rdzenia (bez emisji) — build robi tsconfig.build.json. Rygor z bazy monorepo.
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src"]
}
